apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: agent-web-browser-researcher
  namespace: agent-office
spec:
  to:
    kind: Service
    name: agent-web-browser-researcher
    weight: 100
  port:
    targetPort: gateway
  tls:
    termination: edge
    insecureEdgeTerminationPolicy: Redirect
