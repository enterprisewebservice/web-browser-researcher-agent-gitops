apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: agent-web-browser-researcher-workspace
  namespace: agent-office
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 2Gi
